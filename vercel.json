{
  "buildCommand": "prisma generate && prisma migrate deploy && npm run build",
  "outputDirectory": "build/client",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}

